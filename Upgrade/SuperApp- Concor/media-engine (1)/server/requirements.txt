# Engine media (video_engine.py + photo_engine.py + media_api.py)
# Cài: pip install -r requirements.txt

fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9   # bắt buộc để FastAPI nhận UploadFile (route /upload)
Pillow>=10.0

# Dev/test only — không cần trên máy chạy production của bệnh viện
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27               # FastAPI TestClient cần httpx (hoặc httpx2 tuỳ bản Starlette)
